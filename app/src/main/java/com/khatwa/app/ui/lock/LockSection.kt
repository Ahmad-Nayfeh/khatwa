package com.khatwa.app.ui.lock

import androidx.compose.runtime.Composable
import com.khatwa.app.AppContainer
import com.khatwa.app.steps.Today

/** Home-screen lock controls and health warning. Filled in by the lock phase. */
@Composable
fun LockSection(container: AppContainer, today: Today, onOpenSettings: () -> Unit) = Unit
