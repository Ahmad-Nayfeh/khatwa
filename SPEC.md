# khatwa (خطوة) — تطبيق أندرويد مفتوح المصدر لتتبّع المشي وقفل الجوال (واللابتوب) حتى تمشي

## الهدف
تطبيق أندرويد أصلي، مجاني ومفتوح المصدر لأي مستخدم، يُثبَّت كـ APK بدون قوقل بلاي. يعدّ خطوات
المستخدم كل يوم من حساس الجوال مباشرة، يعرض إحصائيات نظيفة وخفيفة، يعرض حكمة يومية تبعث على
النشاط، ويستطيع قفل الجوال (ما عدا تطبيقات مسموحة يختارها المستخدم) حتى يمشي عدداً محدداً من
الخطوات. ومعه برنامج صغير لويندوز يقفل اللابتوب حتى يكمل هدف اليوم.
أنا (صاحب المستودع) أول مستخدم، لكن كل شيء يجب أن يعمل لأي شخص يثبّته دون أي إعداد خارج التطبيق.

## النشر للعموم
- المستودع عام برخصة MIT (أضف ملف LICENSE). لا بيانات شخصية ولا أسرار في المستودع.
- التنزيل لا يتطلب حساب GitHub: عند دفع وسم إصدار (مثل `v1.0.0`) ينشئ الـ workflow صفحة
  **Release** مرفقاً بها `khatwa-<version>.apk` و`khatwa-laptop-lock-<version>.exe`، وهي رابط
  التنزيل الرسمي في README. artifacts الخاصة بـ Actions تبقى للتطوير فقط.
- شاشة الترحيب داخل التطبيق تشرح كل خطوة (الصلاحيات، الإعدادات المقيّدة، البطارية) لمستخدم
  لا يعرف المشروع؛ لا شيء يتطلب قراءة README ليعمل الجوال.
- كل ما هو شخصي (السر المشترك مع اللابتوب، الأهداف، الحكم المعدّلة) يُولَّد أو يُعدَّل داخل
  التطبيق لكل مستخدم على حدة.

## القيود الصلبة
- مجاني بالكامل: لا اشتراكات، لا إعلانات، لا ذكاء اصطناعي، لا Google Fit، لا Health Connect،
  لا Samsung Health. مصدر الخطوات الوحيد: حساس الجوال.
- كل شيء يعمل بلا إنترنت. الاستثناء الوحيد: ميزة "المجموعات والتنافس" (القسم 10) الاختيارية،
  وهي التي تحتاج الإنترنت وFirebase بخطته المجانية (Spark) فقط. قفل اللابتوب لا يستخدم شبكة أبداً.
- واجهة عربية RTL بالكامل، وضع داكن، بسيطة وهادئة. لا إشعارات مزعجة إلا ما أطلبه أدناه.
- الأجهزة المستهدفة: أي أندرويد 8.0 فأعلى (جهازي للاختبار: سامسونج حديث). اللابتوب: Windows 10/11
  بصلاحيات المسؤول (لابتوب شخصي، لا لابتوب شركة).

## التقنيات
- الجوال: Kotlin + Jetpack Compose + Room + WorkManager. المخططات بمكتبة مجانية (Vico أو
  MPAndroidChart)؛ إن تعذّر تنزيل المكتبة من بيئة البناء (مثلاً jitpack.io محجوب) فارسمها
  بـ Compose Canvas مباشرة وأخبرني. minSdk 26.
- اللابتوب: C# (.NET، WPF أو WinForms) ملف exe واحد مستقل في مجلد `laptop-lock/` داخل نفس المستودع.
- المستودع على GitHub: .gitignore لأندرويد و.NET، لا keystore ولا أسرار في الـ git.

## طريقة البناء والتسليم (إجبارية، لأنني لا أستطيع تثبيت Android SDK أو .NET على أجهزتي)
- افصل المنطق الصرف (عدّ الخطوات وتصفير الإقلاع ومنتصف الليل، الجلسات، الهدف التدريجي،
  السلاسل، HMAC) في وحدة Kotlin/JVM نقية `core/` لها اختبارات وحدة تعمل بلا Android SDK.
- GitHub Actions workflow عند كل push على main وعلى فرع العمل، وعند التشغيل اليدوي، بوظيفتين:
  1. Ubuntu: اختبارات الوحدة، بناء APK (debug + release)، محاكي أندرويد يشغّل الاختبارات
     المطلوبة في معايير القبول، ورفع الأدلة (لقطات شاشة، سجل logcat، قاعدة البيانات) كـ artifact
     باسم `emulator-evidence`، وفحص أن نسخة release تعمل بلا انهيار.
  2. Windows: بناء exe واحد مستقل (`dotnet publish` self-contained single-file) مع اختبارات C#،
     ورفعه كـ artifact.
- الـ APK والـ exe يُرفعان كـ artifacts في كل تشغيل (للتطوير)، وكـ Release عند دفع وسم إصدار
  (للعموم). أعطني روابط كليهما في النهاية.
- التوقيع: أنشئ keystore جديداً خارج git، أرسله لي كملف مع ملف نصي فيه القيم الأربع
  (`KHATWA_KEYSTORE_BASE64`، `KHATWA_KEYSTORE_PASSWORD`، `KHATWA_KEY_ALIAS`، `KHATWA_KEY_PASSWORD`)
  وخطوات إضافتها كـ GitHub Secrets. الـ workflow يوقّع بها إن وُجدت، وإلا بمفتاح debug.
- إن كان مضيف ما محجوباً في بيئة الجلسة (مثل dl.google.com) أخبرني باسمه فوراً حتى أفتحه من
  إعدادات البيئة، ولا تلتفّ عليه. تابع ما لا يعتمد عليه.
- كل مرحلة: commit واضح، وpush، ورابط تشغيل CI أخضر قبل الانتقال للتالية.

## الخطوة صفر
اعرض عليّ خطة قصيرة وانتظر موافقتي قبل كتابة أي كود. اذكر فيها أي نقطة تقنية غير متأكد
منها بدل افتراضها. نقاط أعرف مسبقاً أنها تحتاج حسماً في الخطة:
- كيف تُختبر خدمة القفل على المحاكي: لا أعرف طريقة موثوقة لتغذية Sensor.TYPE_STEP_COUNTER
  من المحاكي. إن لزم مصدر خطوات وهمي في نسخة الاختبار فقط، اقترحه صراحةً.
- Play Protect يحجب تثبيت التطبيقات التي تطلب خدمة الإتاحة أو العرض فوق التطبيقات من خارج
  المتجر، و"الإعدادات المقيّدة" في أندرويد 13+ تحجب تفعيل خدمة الإتاحة، وسامسونج فيه
  Auto Blocker. الحل المعروف: إيقاف فحص Play Protect مؤقتاً عند التثبيت. ضع الخطوات في README.
- ما الذي يمكن إثباته على المحاكي وما الذي يحتاج جهازي الحقيقي (قل ذلك صراحةً في الخطة).

## 1. عدّ الخطوات
- استخدم Sensor.TYPE_STEP_COUNTER (تراكمي منذ آخر إقلاع). خطوات اليوم = القراءة الحالية −
  قراءة بداية اليوم، مع حفظ نقطة البداية في قاعدة البيانات.
- عالج الحالات الحرجة صراحةً: إعادة تشغيل الجوال (يصفّر العدّاد: اكتشف الهبوط في القيمة أو
  استقبل BOOT_COMPLETED وأعد ضبط نقطة البداية دون خسارة ما سبق)، منتصف الليل (إغلاق اليوم
  وفتح يوم جديد حتى لو كان التطبيق مغلقاً)، وتغيير المنطقة الزمنية.
- خدمة أمامية (Foreground Service) خفيفة بإشعار ثابت هادئ يعرض خطوات اليوم والهدف، لأن
  شاشة القفل تحتاج عدّاً شبه لحظي. أضف WorkManager دورياً كاحتياط يحفظ لقطات كل 15 دقيقة.
- خزّن في Room: لكل يوم: التاريخ، الخطوات، وقت آخر تحديث. ولقطات داخل اليوم (كل 15 دقيقة)
  لعرض توزيع المشي على ساعات اليوم.
- الصلاحية: ACTIVITY_RECOGNITION مع شاشة تشرح السبب. وRECEIVE_BOOT_COMPLETED لإعادة تشغيل الخدمة.
- افتراض يظهر في شاشة الترحيب: الحساس يعدّ حركة الجوال، فالجوال يجب أن يكون معي أثناء المشي.

## 2. جلسات المشي
- الجلسة = مشي متواصل 10 دقائق فأكثر بإيقاع مشي حقيقي، مع السماح بتوقفات قصيرة (≤ دقيقتين).
  استنتجها من أحداث الحساس بطوابعها الزمنية (لا من لقطات الـ 15 دقيقة، فهي أخشن من اللازم).
- خزّن لكل جلسة: البداية، النهاية، الخطوات. اعرض في الرئيسية "جلسات هذا الأسبوع: N"،
  وفي الإحصائيات: عدد الجلسات الأسبوعي، أطول جلسة، ومتوسط طول الجلسة.
- الخطوات المتفرقة خارج الجلسات تبقى محسوبة في إجمالي اليوم؛ الجلسات إحصائية إضافية فقط.

## 3. الهدف التدريجي
- الهدف النهائي يحدده المستخدم (افتراضي 8000). الهدف اليومي الفعلي يبدأ منخفضاً ويرتفع تلقائياً.
- أول 7 أيام = أسبوع قياس: الهدف الفعلي = قيمة مؤقتة يحددها المستخدم في الترحيب (افتراضي 3000)،
  والقفل يعمل بها بشكل طبيعي.
- بعد أسبوع القياس: الهدف الفعلي = متوسط خطوات أسبوع القياس + زيادة أسبوعية (افتراضي 500)،
  ثم يرتفع بنفس الزيادة كل أسبوع حتى يصل للهدف النهائي ويثبت عنده.
- إن فشل المستخدم في تحقيق الهدف أكثر من 4 أيام في أسبوع، لا يرتفع الهدف في الأسبوع التالي
  (يثبت) بدل أن يرتفع. لا يهبط تلقائياً أبداً.
- اعرض في الإعدادات: الهدف الفعلي الحالي، الهدف النهائي، الزيادة الأسبوعية، وزر "تعديل يدوي"
  يوقف التدرّج ويثبت هدفاً أختاره.

## 4. سجل الوزن الأسبوعي
- إدخال يدوي للوزن (كجم، بكسر واحد). تذكير أسبوعي اختياري في يوم ووقت أحددهما (افتراضياً مطفأ).
- اعرض في شاشة الإحصائيات خطاً للوزن عبر الأسابيع بجانب مخطط الخطوات الشهري/السنوي.
- لا تحسب BMI ولا سعرات ولا توقعات. مجرد السجل والاتجاه.

## 5. الإحصائيات (خفيفة ونظيفة)
شاشة الرئيسية:
- رقم اليوم كبيراً مع حلقة تقدّم نحو الهدف الفعلي.
- حكمة اليوم (انظر القسم 8).
- الأسبوع الحالي: أعمدة 7 أيام مع خط الهدف. جلسات هذا الأسبوع.
- مؤشر: "هذا الأسبوع مقارنةً بالأسبوع الماضي: ±X%" بلون أخضر/أحمر.
- السلسلة الحالية (أيام متتالية حقّقت فيها الهدف) وأطول سلسلة.
- كود اللابتوب لليوم (يظهر فقط عند اكتمال هدف اليوم، انظر القسم 7).
شاشة الإحصائيات:
- شهري: أعمدة 30 يوماً، متوسط يومي، أفضل يوم، عدد الأيام التي تحقق فيها الهدف، الجلسات.
- آخر 12 شهراً: متوسط الخطوات اليومية لكل شهر + خط الوزن.
- توزيع المشي على ساعات اليوم (متوسط).
- عدّاد الاستسلامات هذا الشهر (انظر القسم 6).
لا تضف مسافات أو سعرات حرارية أو أي تقديرات مشتقة إلا إن طلبتها.

## 6. قفل الجوال (صمّمه بصدق)
### الآلية
- خدمة إتاحة (AccessibilityService) تراقب التطبيق في المقدّمة. عندما يكون القفل نشطاً ويُفتح
  تطبيق خارج قائمة المسموح، تُعرض فوقه فوراً شاشة قفل كاملة عبر SYSTEM_ALERT_WINDOW.
- شاشة القفل تعرض: الخطوات المتبقية بشكل كبير، حلقة تقدّم لحظية، حكمة اليوم، وزر
  "فتح التطبيقات المسموحة" يرجعني للشاشة الرئيسية.
- عند اكتمال الخطوات المطلوبة: القفل يُفتح تلقائياً مع إشعار واحد هادئ "أحسنت".
### وضعان للقفل، يعملان معاً
1. **يدوي:** زر "اقفلني حتى أمشي X خطوة" (1000 / 2000 / 3000 / مخصص). العدّ من لحظة الضغط.
2. **مجدول يومياً:** وقت أحدده (افتراضي 8:00 مساءً). عنده، إذا لم أكمل الهدف الفعلي، يُفعَّل
   القفل تلقائياً حتى أكمله أو حتى وقت انتهاء (افتراضي 11:00 مساءً). خيار أيام الأسبوع.
   تنبيه هادئ قبل التفعيل بـ 30 دقيقة.
### قائمة المسموح
- شاشة فيها كل التطبيقات المثبتة مع بحث وتبديل. افتراضياً مسموح: الهاتف، جهات الاتصال،
  الرسائل، يوتيوب. مسموح دائماً ولا يمكن حظره: المكالمات الواردة، الطوارئ، واجهة النظام،
  المشغّل، وهذا التطبيق.
- خيار "حظر تطبيق الإعدادات أثناء القفل" (افتراضياً مفعّل).
### الهروب (القفل لا يمكن جعله مستحيل الكسر)
- زر "طوارئ / إلغاء القفل": انتظار 60 ثانية، ثم كتابة جملة طويلة، ثم تأكيد. كل إلغاء يُسجَّل
  باسم "استسلام" مع التاريخ والخطوات المتبقية، ويظهر العدّاد في الإحصائيات.
- اكتشف تعطيل خدمة الإتاحة أو صلاحية العرض فوق التطبيقات، واعرض تنبيهاً دائماً في الرئيسية
  "القفل لا يعمل" مع زر يفتح الإعداد. افحص عند كل فتح للتطبيق.
- لا Device Admin ولا Device Owner ولا منع حذف.

## 7. قفل اللابتوب (Windows) — بدون أي شبكة
### المبدأ
كود يومي يُحسب من (سر مشترك + تاريخ اليوم). الجوال يعرضه فقط بعد اكتمال هدف اليوم. اللابتوب
يطلبه عند تسجيل الدخول. لا واي فاي، لا بلوتوث، لا سيرفر.
### جهة الجوال
- في الإعدادات: "قفل اللابتوب" → يولّد سراً عشوائياً (24 حرفاً) ويعرضه مرة واحدة لأنسخه إلى
  اللابتوب، مع زر إعادة توليد.
- الكود = أول 6 أرقام من HMAC-SHA256(secret, "YYYY-MM-DD" بالتاريخ المحلي). يظهر في الرئيسية
  فقط عندما تكون خطوات اليوم ≥ الهدف الفعلي. قبل ذلك تظهر "الكود يظهر بعد إكمال هدف اليوم"
  مع الخطوات المتبقية.
### جهة اللابتوب (`laptop-lock/`)
- exe واحد بدون تثبيت، يسجّل نفسه كمهمة مجدولة عند تسجيل الدخول (بأعلى صلاحيات) ويشغّل
  نسخة مراقبة تعيد فتحه إن أُغلق.
- عند تسجيل الدخول: إن لم يُدخل كود اليوم بعد → نافذة كاملة الشاشة، فوق كل النوافذ، تمنع الوصول
  لسطح المكتب قدر الإمكان (Topmost، منع Alt+Tab، تعطيل Win key حيث أمكن). تعرض:
  "أكمل تحدي المشي ثم أدخل الكود من تطبيق khatwa". حقل الكود + زر طوارئ.
- يقبل كود اليوم، وكذلك كود الأمس (هامش لاختلاف التاريخ بين الجهازين). عند القبول يحفظ
  "مفتوح حتى نهاية اليوم" في ملف محلي ويختفي. في اليوم التالي يقفل من جديد.
- الطوارئ بنفس مقاومة الجوال: 60 ثانية + كتابة جملة + تأكيد، وتسجيل الاستسلام في ملف محلي
  يعرضه البرنامج في نافذة صغيرة عند الطلب.
- ملف إعدادات بسيط (JSON) فيه السر، ووقت انتهاء القفل المسائي إن أردته لاحقاً (غير مطلوب الآن).
- صراحةً: قابل للكسر (الوضع الآمن، إدارة المهام، الحذف). لا تحاول منع ذلك بحيل عميقة في النظام؛
  المطلوب مقاومة معقولة فقط.
- أضف سكربت `install.ps1` وواحداً `uninstall.ps1`.

## 8. الحكم اليومية
- قائمة مضمّنة (assets/quotes.json) بالعربية، 365 حكمة على الأقل، عن النشاط والمشي والانضباط
  والصبر والصحة. حكمة ثابتة لكل يوم، مع زر "حكمة أخرى".
- قاعدة صارمة: لا تنسب أي حكمة لشخص حقيقي أو نص ديني إلا إن كنت متأكداً من صحة النص والنسبة
  حرفياً. عند أدنى شك اكتبها بلا نسبة. حكمة بلا نسبة أفضل من نسبة مختلقة.
- قابلة للتعديل من الإعدادات، وتصدير/استيراد JSON.
- إشعار صباحي اختياري بحكمة اليوم وخطوات الأمس. افتراضياً مطفأ.

## 9. الإعدادات
الهدف النهائي والزيادة الأسبوعية والهدف المؤقت، القفل المجدول (الوقت، الانتهاء، الأيام)، قائمة
المسموح، حظر الإعدادات أثناء القفل، قفل اللابتوب (السر)، تذكير الوزن، الإشعار الصباحي، الحكم،
نسخة احتياطية كاملة (تصدير/استيراد JSON يشمل الخطوات والجلسات والوزن والقواعد)، مسح البيانات،
الوضع الداكن، وشاشة حالة الصلاحيات (الحساس، الإتاحة، العرض فوق التطبيقات، استبعاد البطارية).

## 10. المجموعات والتنافس (اختيارية، عبر Firebase بخطة Spark المجانية)
### المبدأ
- كل ما سبق يعمل بلا إنترنت. هذه الميزة تُفعَّل من الإعدادات فقط، وحينها يرسل التطبيق خطوات
  اليوم إلى Firebase (Firestore) عدة مرات في اليوم. من لا يفعّلها لا يخرج من جواله أي بيانات.
- الهوية: تسجيل دخول مجهول (Firebase Anonymous Auth) بلا بريد ولا كلمة مرور، مع اسم مستعار
  يختاره المستخدم. لأن الهوية المجهولة تضيع عند حذف التطبيق، ضمّن الهوية في النسخة الاحتياطية
  وأضف زر "ربط بحساب Google" (مجاني في Firebase) لمن يريد استعادة حسابه على جهاز آخر.
### المجموعات
- أي مستخدم ينشئ مجموعة (اسم + وصف قصير) فيصبح مالكها ويحصل على **كود دعوة** (8 أحرف) يعطيه
  لمن يشاء. من يدخل الكود ينضم. المالك يستطيع: إعادة توليد الكود، إزالة عضو، حذف المجموعة.
  أي عضو يستطيع المغادرة. المستخدم قد يكون في أكثر من مجموعة.
- داخل المجموعة (للأعضاء فقط): ترتيب الأفراد بالاسم المستعار والخطوات، مع مجموع المجموعة وعدد
  الأعضاء الذين حققوا هدفهم اليوم. فلاتر الترتيب:
  - الفترة: اليوم / هذا الأسبوع / هذا الشهر.
  - الترتيب حسب: الخطوات، عدد الأيام التي تحقق فيها الهدف في الفترة، السلسلة الحالية، أطول جلسة
    مشي في الفترة. تصاعدي/تنازلي، فيظهر الأفضل والأسوأ.
### التنافس العام بين المجموعات
- شاشة "المجموعات" العامة يراها أي مستخدم للتطبيق، وفيها كل المجموعات باسمها وعدد أعضائها فقط،
  مع بحث بالاسم. فلاتر الترتيب:
  - الفترة: اليوم / هذا الأسبوع / هذا الشهر.
  - الترتيب حسب: مجموع الخطوات، متوسط الخطوات لكل عضو (حتى لا تفوز المجموعات الكبيرة بالعدد
    وحده)، نسبة الأعضاء الذين حققوا هدفهم، عدد الأعضاء. تصاعدي/تنازلي.
  - الافتراضي: هذا الأسبوع، حسب المجموع، تنازلياً.
- غير الأعضاء لا يرون أسماء الأعضاء ولا أرقامهم ولا كود الدعوة. يُفرض ذلك بقواعد أمان
  Firestore على السيرفر، لا في واجهة التطبيق فقط.
- خيار للمالك: "إخفاء المجموعة من القائمة العامة" (افتراضياً ظاهرة).
### حدود اعرفها ويجب أن تُحسم في خطة هذه المرحلة
- خطة Spark لا تسمح بتشغيل كود على السيرفر (Cloud Functions تحتاج بطاقة). لذلك مجموع المجموعة
  يُحسب من كتابات الأجهزة نفسها: كل عضو يكتب مساهمته في مستند عام للمجموعة تحت معرّف مجهول.
  النتيجة: مستخدم تقني خارج المجموعة يستطيع، عبر الـ API لا عبر التطبيق، رؤية أرقام مجهولة
  الهوية بلا أسماء. اذكر هذا صراحةً في README. إن وجدت تصميماً أفضل بلا سيرفر فاعرضه.
- لا يوجد في التطبيق أي إدخال يدوي للخطوات، لا في المجموعات ولا في غيرها؛ المصدر الوحيد هو
  حساس الجوال. لكن الرقم يُرسل من جهاز المستخدم نفسه، ولا يمكن بلا سيرفر منع مستخدم تقني من
  التلاعب خارج التطبيق. لا تحاول منع ذلك بحيل معقدة؛ اكتفِ بحد أقصى معقول لليوم (مثل 60 ألف
  خطوة) تُرفض القيم فوقه بقواعد Firestore.
- الحماية من الإساءة (إنشاء آلاف المجموعات، استنزاف الحصة المجانية): فعّل Firebase App Check
  المجاني، وقيّد بقواعد Firestore: حجم المستند، عدد المجموعات لكل مستخدم (مثلاً 5)، وعدد
  الأعضاء لكل مجموعة (مثلاً 200).
- إعداد Firebase من طرفي: أنا أنشئ المشروع من المتصفح (console.firebase.google.com)، وأنت
  تعطيني خطوات دقيقة: تسجيل تطبيق أندرويد باسم الحزمة، تنزيل google-services.json (يُخزَّن
  كـ GitHub Secret بترميز base64، لا في المستودع العام)، وملف قواعد Firestore الذي ألصقه في
  لوحة التحكم بنفسي. وأخبرني بأي مضيف يحتاجه البناء إن كان محجوباً.
- الاختبار في CI بلا مشروع حقيقي: استخدم Firebase Emulator Suite (مجاني ومحلي) على runner
  الـ Ubuntu لاختبار قواعد الأمان (غير العضو لا يقرأ الأعضاء) ومنطق الانضمام والترتيب.
### الصلاحيات الإضافية
INTERNET وACCESS_NETWORK_STATE فقط، ويُذكر في شاشة تفعيل الميزة ما الذي يُرسل بالضبط.

## الصلاحيات (الجوال)
ACTIVITY_RECOGNITION، FOREGROUND_SERVICE (+ النوع المناسب)، SYSTEM_ALERT_WINDOW،
BIND_ACCESSIBILITY_SERVICE، RECEIVE_BOOT_COMPLETED، POST_NOTIFICATIONS،
QUERY_ALL_PACKAGES. وINTERNET وACCESS_NETWORK_STATE لأجل القسم 10 فقط.
أندرويد 13+ يحجب خدمات الإتاحة للتطبيقات من خارج المتجر عبر "الإعدادات المقيّدة": اكتشف
الحالة واعرض خطوات فتحها داخل التطبيق.

## معايير القبول (نفّذها وأرني الدليل)
- اختبارات وحدة: إعادة تشغيل الجوال لا تخسر خطوات ولا تضاعفها؛ منتصف الليل صحيح؛ الأسبوع/الشهر/
  السلسلة صحيحة؛ اكتشاف الجلسات على بيانات وهمية (مشي 12 دقيقة = جلسة، 6 دقائق = لا)؛
  الهدف التدريجي يرتفع ويثبت حسب القواعد؛ كود HMAC يتطابق بين تنفيذ Kotlin وتنفيذ C# لنفس
  السر والتاريخ (اختبار مشترك بقيم ثابتة يعمل في وظيفتَي CI معاً).
- على محاكي بحساس وهمي: القفل اليدوي يمنع تطبيقاً غير مسموح، يسمح بيوتيوب والهاتف، ويُفتح
  عند اكتمال الخطوات. المجدول يُفعَّل في وقته إن لم يكتمل الهدف.
- الطوارئ (جوال ولابتوب) تتطلب الانتظار والكتابة وتسجّل استسلاماً.
- اكتشاف تعطيل خدمة الإتاحة يعرض التنبيه.
- على ويندوز: بعد install.ps1 وإعادة تسجيل الدخول تظهر شاشة القفل، الكود الصحيح يفتحها،
  الخاطئ لا يفتحها، وإغلاق النافذة يعيد فتحها. (ما لا يمكن إثباته على runner ويندوز في CI،
  قل إنه يحتاج تجربتي على لابتوبي.)
- المجموعات (القسم 10): اختبارات على Firebase Emulator في CI تثبت أن غير العضو لا يستطيع قراءة
  أعضاء المجموعة ولا كود دعوتها، وأن الترتيب والمجاميع صحيحة، وأن الأرقام فوق الحد الأقصى تُرفض.
- ابنِ APK موقّعاً وexe وأعطني روابط تنزيلهما من CI.

## طريقة العمل
- ابدأ بالخطوة صفر. ثم اعرض خطة قصيرة وانتظر موافقتي.
- مراحل: (1) الحساس + الخدمة + التخزين + الإقلاع + منتصف الليل، (2) الرئيسية والإحصائيات +
  الجلسات + الهدف التدريجي + الوزن، (3) القفل اليدوي + المسموح + شاشة القفل + الطوارئ،
  (4) القفل المجدول، (5) الحكم + الإعدادات + النسخ الاحتياطي + APK، (6) قفل اللابتوب،
  (7) المجموعات والتنافس عبر Firebase (تبدأ فقط بعد أن تعمل المراحل 1–6 على جوالي).
  أرني كل مرحلة تعمل قبل التالية، وcommit واضح بعد كل مرحلة.
- لا تضف ميزات لم أطلبها. إن رأيت شيئاً ضرورياً، اقترحه ولا تنفّذه بصمت.
- في النهاية README.md موجّه لأي مستخدم عادي (بالعربية، مع ملخص قصير بالإنجليزية في الأعلى):
  1. ما التطبيق وما الذي لا يفعله (يعمل بلا إنترنت؛ لا يُرسل شيء إلا إن فعّلت المجموعات، وحينها
     يُرسل الاسم المستعار وخطوات اليوم فقط).
  2. **الجوال**: تحميل الـ APK من صفحة Releases، التثبيت (السماح بالمصدر غير المعروف، إيقاف فحص
     Play Protect مؤقتاً ثم إعادته، Auto Blocker على سامسونج)، فتح الإعدادات المقيّدة على
     أندرويد 13+، تفعيل خدمة الإتاحة، صلاحية العرض فوق التطبيقات، استبعاد التطبيق من تحسين
     البطارية على سامسونج (وإضافته لقائمة "التطبيقات غير الخاضعة للسكون")، وأخذ نسخة احتياطية.
  3. **اللابتوب**: تحميل الـ exe من Releases، تحذير SmartScreen وكيف يُتجاوز، نسخ السر من
     التطبيق، تشغيل install.ps1 (بصلاحيات المسؤول)، التجربة الأولى، وuninstall.ps1 للإزالة.
  4. الأسئلة الشائعة: القفل لا يعمل، الخطوات لا تُعدّ، فقدت السر، كيف أحذف كل شيء.
  5. للمطوّرين (قسم قصير في الأسفل): البناء عبر GitHub Actions، وإضافة أسرار التوقيع.
  خطوة بخطوة، بلغة بسيطة، وبلا افتراض أن القارئ يعرف GitHub.
